"""Service layer.

Business logic lives here, kept separate from API routing (``app/api``) and the
persistence layer (``app/db``/``app/models``). Feature tickets add modules such
as ``app/services/project.py``.
"""
